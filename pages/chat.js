import Sidebar from '../components/Sidebar';

const Chat = () => {
    return (
        <div>
            <Sidebar />
            <h1>this is the chat page</h1>
        </div>
    );
}

export default Chat;